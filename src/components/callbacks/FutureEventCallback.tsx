import React, { useState } from "react";

const FutureEventCallback = () => {
  const [edit, setEdit] = useState(false);

  return (
    <div>
      <h1>Future Event Created</h1>
    </div>
  );
};

export default FutureEventCallback;
